export interface ExampleType {
    id: number;
    name: string;
    isActive: boolean;
}

export interface AppProps {
    title: string;
}

export type User = {
    id: string;
    username: string;
    email: string;
};